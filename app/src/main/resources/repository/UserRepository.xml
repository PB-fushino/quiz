<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="jp.co.plan_b.training.infractstucture.repository.user.UserRepository">
    <resultMap id="ResultTest" type="jp.co.plan_b.training.model.dto.User">

    </resultMap>

    <select id="getAll"  resultMap="ResultTest">
        SELECT
         *
      	FROM
      	 user
    </select>

    <select id="getUserByName"  resultMap="ResultTest">
        SELECT
         *
      	FROM
      	 user
      	 where
      	 name = #{name}
    </select>

    <select id="getUserById"  resultMap="ResultTest">
        SELECT
         *
      	FROM
      	 user
      	 where
      	 id = #{id}
    </select>

    <insert id ="registerUser" >
    insert into user

    (name)

    values(#{name})



    </insert>


</mapper>